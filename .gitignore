# Source footage, kept locally. Nothing in the deck, the film or the lab references it, and at
# 73 MB it would turn a 6 MB site into an 80 MB clone for every reader.
Indexed_Confluence_Docs_Agent_compressed.mp4
*.mp4

# Planning transcript from the session that fixed the film's stages 2, 6 and 9. The fixes shipped;
# the notes describing them as broken did not age into anything a reader wants.
enhancement_plan.txt

# Working context for future sessions. The repo is the finished resource; this is the workshop.
CLAUDE.md
.claude.local.md

# OS clutter
Thumbs.db
ehthumbs.db
desktop.ini
.DS_Store

# Promo brief for pitching the session at other events. Written for LinkedIn/Luma copy, not for
# readers of the finished resource.
EVENT-BRIEF.md

# Local scratch output from verify-notebook.py — the executed/verified notebook copies it
# writes for inspection are working state, not the deliverable.
scratch_exec/
__pycache__/
